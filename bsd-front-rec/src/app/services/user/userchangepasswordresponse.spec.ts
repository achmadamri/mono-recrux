import { UserChangePasswordResponse } from './userchangepasswordresponse';

describe('UserChangePasswordResponse', () => {
  it('should create an instance', () => {
    expect(new UserChangePasswordResponse()).toBeTruthy();
  });
});
