import { UserChangePasswordRequest } from './userchangepasswordrequest';

describe('UserChangePasswordRequest', () => {
  it('should create an instance', () => {
    expect(new UserChangePasswordRequest()).toBeTruthy();
  });
});
