import { UserRegisterResponse } from './userregisterresponse';

describe('UserRegisterResponse', () => {
  it('should create an instance', () => {
    expect(new UserRegisterResponse()).toBeTruthy();
  });
});
