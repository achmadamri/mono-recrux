import { GetEntryListResponse } from './getentrylistresponse';

describe('GetEntryListResponse', () => {
  it('should create an instance', () => {
    expect(new GetEntryListResponse()).toBeTruthy();
  });
});
