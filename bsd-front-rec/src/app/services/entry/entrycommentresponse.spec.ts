import { EntryCommentResponse } from './entrycommentresponse';

describe('EntryCommentResponse', () => {
  it('should create an instance', () => {
    expect(new EntryCommentResponse()).toBeTruthy();
  });
});
