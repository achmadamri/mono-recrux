import { GetDepartmentResponse } from './getdepartmentresponse';

describe('GetDepartmentResponse', () => {
  it('should create an instance', () => {
    expect(new GetDepartmentResponse()).toBeTruthy();
  });
});
