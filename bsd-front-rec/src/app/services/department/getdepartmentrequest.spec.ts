import { GetDepartmentRequest } from './getdepartmentrequest';

describe('GetDepartmentRequest', () => {
  it('should create an instance', () => {
    expect(new GetDepartmentRequest()).toBeTruthy();
  });
});
