package com.api.rec.departments.db.repository;

import org.springframework.data.jpa.repository.JpaRepository;

import com.api.rec.departments.db.entity.ViewDashJobResume;

public interface ViewDashJobResumeRepository extends JpaRepository<ViewDashJobResume, Integer> {
}